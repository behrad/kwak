import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:toggle-message', 'ToggleMessageView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
