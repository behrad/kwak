<nav class="navbar navbar-default navbar-fixed-top" role="navigation">
  <div class="container-fluid">
    <div class="navbar-header">
      <i class="navbar-toggle glyphicon glyphicon-off" {{action 'invalidateSession'}}></i>
      {{#if model.is_admin}}
        {{#link-to 'channels.admin'}}<i class="navbar-toggle glyphicon glyphicon-edit"></i>{{/link-to}}
      {{/if}}
      <i class="navbar-toggle glyphicon glyphicon-refresh" {{action 'reload'}}></i>
      {{#link-to "channels" classNames="navbar-brand"}}<img src="assets/images/kwak.png">{{/link-to}}
    </div>

    <div class="collapse navbar-collapse">
      {{#if session.isAuthenticated}}
        <div class="navbar-form navbar-right">
          <small class="text-muted">{{model.email}}</small>
          {{#if model.is_admin}}
            {{#link-to 'channels.admin' class="btn btn-default"}}<i class="glyphicon glyphicon-edit"></i> Admin{{/link-to}}
          {{/if}}
          <a {{action 'invalidateSession'}} class="btn btn-default"><i class="glyphicon glyphicon-off"></i> Logout</a>
        </div>
      {{/if}}
    </div><!-- /.navbar-collapse -->
  </div>
</nav>

{{outlet}}
