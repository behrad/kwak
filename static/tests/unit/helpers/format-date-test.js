import {
  formatDate
} from 'static/helpers/format-date';

module('FormatDateHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = formatDate(42);
  ok(result);
});
